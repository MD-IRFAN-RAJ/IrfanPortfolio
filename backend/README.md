Backend for Portfolio Resume site

Quick start

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies: from `backend` run `npm install`.
3. Create admin user manually in MongoDB or contact admin to set up.
4. Start server: `npm run dev` (needs nodemon) or `npm start`.

